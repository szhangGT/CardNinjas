using System.Collections;
using UnityEngine;

namespace Assets.Script.Weapons
{
    class EnemyBullet : BulletControl
    {
        void Start()
        {
            moveDir = Vector3.forward;
            Destroy(gameObject, lifeTime);
        }
    }
}